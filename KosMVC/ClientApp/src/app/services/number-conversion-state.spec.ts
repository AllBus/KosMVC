import { NumberConversionState } from './number-conversion-state';

describe('NumberConversionState', () => {
  it('should create an instance', () => {
    expect(new NumberConversionState()).toBeTruthy();
  });
});
